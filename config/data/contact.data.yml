parameters:
    contact:
        hotline: '%env(CONTACT_HOTLINE)%'
        telephone: '%env(CONTACT_TELEPHONE)%'
        name: '%env(CONTACT_NAME)%'
        address: '%env(CONTACT_ADDRESS)%'
        mail: '%env(CONTACT_MAIL)%'
        facebook: '%env(CONTACT_FACBOOK_LINK)%'
        youtube: '%env(CONTACT_YOUTUBE_LINK)%'
        instagram: '%env(CONTACT_INSTAGRAM)%'